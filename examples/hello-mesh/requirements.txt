# hello-mesh Python dependencies
# Install: pip install -r requirements.txt

# gRPC runtime (for future stub calls)
grpcio>=1.81.1
grpcio-tools>=1.81.1
# grpcio-tools 1.81.1 (latest stable) requires protobuf>=6.33.5,<7.0.0 (verified on
# PyPI). The prior >=7.35.1,<8.0.0 pin conflicted with that range, so pip raised
# ResolutionImpossible. Aligned to grpcio-tools 1.81.1's actual protobuf requirement.
protobuf>=6.33.5,<7.0.0

# Ed25519 signing (production — replaces placeholder in hello_mesh.py)
cryptography>=49.0.0
